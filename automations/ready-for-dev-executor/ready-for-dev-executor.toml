version = 1
id = "ready-for-dev-executor"
name = "Ready-for-Dev Executor"
prompt = '''Use gpt-5.3-codex with reasoning effort medium.

Run these commands from repository root:
`pnpm install`
`pnpm software-factory doctor`
`pnpm software-factory operation run ready-for-dev-executor`

Contract:
- This wrapper is automation-only.
- Do not re-implement routing/execution logic in this prompt.
- The Software Factory CLI is source of truth for operation selection, model/thinking routing, and launch behavior.
- Return a concise summary with routed operation, command status, and no-op reason if any.'''
status = "ACTIVE"
rrule = "RRULE:FREQ=HOURLY;INTERVAL=1;BYMINUTE=11;BYDAY=SU,MO,TU,WE,TH,FR,SA"
cwds = ["$REPO_ROOT"]
created_at = 1771680075906
updated_at = 1771798046000
