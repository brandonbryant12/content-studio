version = 1
id = "issue-evaluator"
name = "Issue Evaluator"
prompt = '''Use gpt-5.3-codex with reasoning effort xhigh.

Run this command from repository root:
`pnpm software-factory operation run issue-evaluator --model gpt-5.3-codex --thinking xhigh`

Contract:
- This command is automation-only.
- Keep execution lightweight and advisory: no dedicated worktree, no install/build/test gates unless a human explicitly switches to code-writing mode.
- Do not re-implement routing/execution logic in this prompt.
- The Software Factory CLI is source of truth for operation selection, model/thinking routing, and launch behavior.
- Return a concise summary with routed operation, command status, and no-op reason if any.'''
status = "ACTIVE"
rrule = "RRULE:FREQ=HOURLY;INTERVAL=1;BYMINUTE=5;BYDAY=SU,MO,TU,WE,TH,FR,SA"
cwds = ["$REPO_ROOT"]
created_at = 1771879712000
updated_at = 1771980000000
