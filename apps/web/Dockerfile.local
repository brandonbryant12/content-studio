# Simple Dockerfile for local testing with pre-built dist
# Usage: Build web app locally first (pnpm build), then:
#   docker build -t content-studio-web:local -f apps/web/Dockerfile.local .

FROM nginx:stable-alpine

WORKDIR /app

COPY apps/web/nginx.conf /etc/nginx/nginx.conf
COPY apps/web/dist /usr/share/nginx/html

HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
  CMD curl --fail --silent http://0.0.0.0:80/healthcheck || exit 1

CMD ["nginx", "-g", "daemon off;"]
