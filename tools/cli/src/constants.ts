export const PRODUCT_NAME = 'Creator Studio' as const;

export const ProductBranding = {
  PRODUCT_NAME,
  CLI_NAME: `${PRODUCT_NAME} CLI`,
  DEVELOPER_CLI_DESCRIPTION: `${PRODUCT_NAME} developer CLI`,
} as const;
